#! package-manifest: ./deploy/chart/catalog_resources/rh-operators/myoperatorname.v4.2.0.clusterserviceversion.yaml
packageName: my-operator-name
channels:
- name: 4.1
# this value needs to be replaced by the ART pipeline but i don't think we currently have a mechanism to do so
  currentCSV: myoperatorname.v4.1.0
- name: 4.2
# this value is replaced by the ART pipeline
  currentCSV: myoperatorname.v4.2.0
